included:
  - Gameboxd
  - GameboxdTests
  - GameboxdWidget

excluded:
  - Gameboxd.xcodeproj
  - Docker
  - DerivedData

disabled_rules:
  - trailing_whitespace
  - line_length

opt_in_rules:
  - empty_count
  - closure_spacing
  - force_unwrapping
